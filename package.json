{
  "name": "eth2023-chainfeed",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ eth2023-chainfeed",
    "create-local": "graph create --node http://localhost:8020/ eth2023-chainfeed",
    "remove-local": "graph remove --node http://localhost:8020/ eth2023-chainfeed",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 eth2023-chainfeed",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.50.1",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
